# Microsoft 365 SMTP — used by /api/contact to send enquiry emails
# In production: set these in Hostinger hPanel → Node.js website → Environment variables
# In local dev: copy this file to .env.local and fill in values (.env.local is gitignored)

SMTP_HOST=smtp.office365.com
SMTP_PORT=587
SMTP_USER=s.vivek@savitursolutions.com
SMTP_PASS=
SMTP_FROM=s.vivek@savitursolutions.com
